class WynonaService:
    """WhisperX transcription service on WYNONA GPU server (stub)."""

    async def transcribe(self, session_id: str, audio_url: str) -> dict:
        raise NotImplementedError("WynonaService.transcribe() not yet implemented")
