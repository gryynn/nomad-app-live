node_modules/
dist/
.env
__pycache__/
*.pyc
.venv/
*.egg-info/
.DS_Store
.vscode/
*.sqlite3
.claude/

# Auto Claude
.auto-claude/
.auto-claude-security.json
.auto-claude-status
.claude_settings.json
.worktrees/
.security-key
logs/security/

# Auto-Claude generated artifacts (without dot prefix)
auto-claude/
context.txt
gitleaks-report.json

# Local Docker Compose overrides (host-specific paths, traefik labels, ...)
docker-compose.override.yml
docker-compose.override.*.yml
!docker-compose.override.example.yml

# Local-only audio storage fallback (when no override exists)
data/

# Backup files generated during ops sessions
*.bak
*.bak-*

# Deploy logs (rotated by hand, not by git)
deploy.log
